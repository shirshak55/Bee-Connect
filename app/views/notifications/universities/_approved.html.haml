%li{class: "#{"notification-unread" if !notification.read_at?}"}
  = link_to [notification.notifiable],class: 'dropdown-item' do
    = "#{notification.actor.full_name} approved you at  #{notification.notifiable.name} university"